import { Plugin } from 'vuepress/core';

/**
 * Options for @vuepress/plugin-google-tag-manager
 */
interface GoogleTagManagerPluginOptions {
    /**
     * The Container ID of Google Tag Manager which should start with `'GTM-'`.
     */
    id: string;
}
declare const googleTagManagerPlugin: (options: GoogleTagManagerPluginOptions) => Plugin;

export { type GoogleTagManagerPluginOptions, googleTagManagerPlugin };
